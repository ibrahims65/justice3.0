<%_ locals.layout = false; %>
<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta 
    name="viewport" 
    content="width=device-width, initial-scale=1.0, maximum-scale=1.0" 
  />
  <title>Unified Justice System – Login</title>
  <link 
    href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" 
    rel="stylesheet" 
  />
  <link 
    href="https://fonts.googleapis.com/css2?family=Scheherazade+New&display=swap" 
    rel="stylesheet" 
  />
  <style>
    body { font-family: 'Scheherazade New', serif; }
  </style>
</head>
<body 
  class="relative bg-cover bg-center min-h-screen overflow-hidden text-white" 
  style="background-image: url('/images/justice_logo.jpg');"
>
  <!-- stronger dark overlay + backdrop blur -->
  <div 
    class="absolute inset-0 bg-black bg-opacity-60 backdrop-blur-md z-0"
  ></div>

  <div 
    class="relative z-10 flex flex-col items-center justify-center min-h-screen px-4"
  >
    <!-- glass-morphic card -->
    <div 
      class="max-w-md w-full bg-white bg-opacity-10 backdrop-filter backdrop-blur-lg 
             rounded-2xl shadow-xl p-8 space-y-6 transform transition hover:scale-105"
    >
      <!-- emblem + titles -->
      <div class="flex flex-col items-center space-y-3">
        <img 
          src="/images/logo1.png" 
          alt="Somaliland Emblem" 
          class="w-20 h-20 sm:w-24 sm:h-24 rounded-full shadow-md" 
        />
        <h1 class="text-2xl sm:text-3xl font-bold text-center tracking-tight">
          Republic of Somaliland
        </h1>
        <h2 class="text-lg sm:text-xl font-semibold text-center text-gray-200">
          Unified Justice System
        </h2>
      </div>

      <!-- verse -->
      <div class="text-center space-y-2 px-2">
        <p 
          class="text-base sm:text-lg font-semibold text-gray-100" 
          lang="ar" translate="no"
        >
          إِنَّ ٱللَّهَ يَأْمُرُكُمْ أَن تُؤَدُّوا۟ ٱلْأَمَـٰنَـٰتِ…
        </p>
        <p class="text-sm sm:text-base italic text-gray-300">
          “Indeed, Allah commands you to render trusts… judge with justice.”
        </p>
      </div>

      <!-- form -->
      <div class="space-y-4">
        <% if (error_msg) { %>
          <div class="bg-red-500 text-white p-3 rounded">
            <%= Array.isArray(error_msg) ? error_msg[0] : error_msg %>
          </div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-4">
          <div>
            <label 
              for="username" 
              class="block text-sm font-medium mb-1"
            >
              Username
            </label>
            <input
              id="username"
              name="username"
              type="text"
              value="policetestuser"
              required
              class="w-full px-3 py-2 rounded-lg bg-gray-800 border border-gray-600 
                     text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your username"
            />
          </div>

          <div>
            <label 
              for="password" 
              class="block text-sm font-medium mb-1"
            >
              Password
            </label>
            <input
              id="password"
              name="password"
              type="password"
              required
              class="w-full px-3 py-2 rounded-lg bg-gray-800 border border-gray-600 
                     text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Enter your password"
            />
          </div>

          <div class="flex flex-col sm:flex-row items-center justify-between space-y-2 sm:space-y-0">
            <button
              type="submit"
              class="w-full sm:w-auto bg-blue-600 hover:bg-blue-700 
                     text-white font-bold py-2 px-6 rounded-lg transition"
            >
              Sign In
            </button>
            <a 
              href="/auth/register" 
              class="text-sm font-medium text-blue-300 hover:text-blue-500"
            >
              Register
            </a>
          </div>
        </form>
      </div>

      <!-- footer note -->
      <p class="text-xs text-gray-300 text-center">
        Authorized users only. Unauthorized access may be prosecuted.
      </p>
    </div>
  </div>
</body>
</html>
