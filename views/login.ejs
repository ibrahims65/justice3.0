<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Unified Justice System - Login</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Scheherazade+New&display=swap" rel="stylesheet" />
  <style>
    @keyframes scroll {
      0% { transform: translateX(100%); }
      100% { transform: translateX(-100%); }
    }
    .animate-scroll {
      animation: scroll 30s linear infinite;
    }
    body {
      font-family: 'Scheherazade New', serif;
    }
  </style>
</head>
<body class="relative bg-cover bg-center min-h-screen text-white" style="background-image: url('/images/justice_logo.jpg');">
  <!-- Dark overlay -->
  <div class="absolute inset-0 bg-black bg-opacity-60 z-0"></div>

  <!-- Login container -->
  <div class="relative z-10 flex flex-col items-center justify-center min-h-screen px-4">
    <!-- Emblem -->
    <img src="/images/logo1.png" alt="Somaliland Emblem" class="w-24 h-24 rounded-full shadow-lg mb-4" />

    <!-- Title -->
    <h1 class="text-2xl font-bold text-center tracking-wide">Republic of Somaliland</h1>
    <h2 class="text-xl font-semibold text-center text-gray-200 mb-6">Unified Justice System</h2>

    <!-- Quranic Verse (Arabic Only, Scrolling) -->
    <div class="overflow-hidden whitespace-nowrap w-full mb-6">
      <p class="inline-block animate-scroll text-lg font-semibold text-gray-100">
        إِنَّ ٱللَّهَ يَأْمُرُكُمْ أَن تُؤَدُّوا۟ ٱلْأَمَـٰنَـٰتِ إِلَىٰٓ أَهْلِهَا ۖ وَإِذَا حَكَمْتُم بَيْنَ ٱلنَّاسِ أَن تَحْكُمُوا۟ بِٱلْعَدْلِ ۚ إِنَّ ٱللَّهَ نِعِمَّا يَعِظُكُم بِهِۦٓ ۗ إِنَّ ٱللَّهَ كَانَ سَمِيعًۭا بَصِيرًۭا
      </p>
    </div>

    <!-- Login Form -->
    <div class="w-full max-w-md bg-white bg-opacity-10 rounded-lg shadow-lg p-8">
      <form action="/auth/login" method="POST">
        <div class="mb-4">
          <label for="username" class="block text-sm font-bold mb-2">Username</label>
          <input type="text" name="username" id="username" value="policetestuser" class="w-full px-3 py-2 rounded bg-gray-800 text-white border border-gray-600 focus:outline-none focus:ring focus:border-blue-500" placeholder="Enter your username" />
        </div>
        <div class="mb-6">
          <label for="password" class="block text-sm font-bold mb-2">Password</label>
          <input type="password" name="password" id="password" class="w-full px-3 py-2 rounded bg-gray-800 text-white border border-gray-600 focus:outline-none focus:ring focus:border-blue-500" placeholder="Enter your password" />
        </div>
        <div class="flex items-center justify-between">
          <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition">
            Sign In
          </button>
          <a href="/auth/register" class="text-sm font-bold text-blue-400 hover:text-blue-600">Register</a>
        </div>
      </form>

      <!-- Disclaimer -->
      <p class="text-xs text-gray-300 mt-6 text-center">
        Authorized users only. Unauthorized access may be prosecuted.
      </p>
    </div>
  </div>
</body>
</html>
