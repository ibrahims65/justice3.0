<%- include('../partials/header', { title: 'New Remand Request', user: user, page: '/remand' }) %>
<div class="container mx-auto p-8">
  <h1 class="text-2xl font-bold mb-4">New Remand Request for Booking #<%= bookingId %></h1>
  <form action="/remand" method="POST">
      <input type="hidden" name="bookingId" value="<%= bookingId %>">
      <div class="bg-white p-6 rounded-lg shadow-md">
        <div class="mb-4">
          <label for="reason" class="block text-gray-700">Reason</label>
          <textarea name="reason" id="reason" class="w-full border rounded p-2" required></textarea>
        </div>
        <div class="mb-4">
          <label for="requestedDays" class="block text-gray-700">Requested Days</label>
          <input type="number" name="requestedDays" id="requestedDays" value="7" class="w-full border rounded p-2" required>
        </div>
        <button type="submit" class="bg-blue-500 text-white p-2 rounded">Submit Request</button>
      </div>
    </form>
  </div>
</body>
</html>
