<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>E-file Document</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <%- include('../partials/navbar', { user: user, page: '/efile/new' }) %>
  <div class="container mx-auto p-8">
    <h1 class="text-2xl font-bold mb-4">E-file Document</h1>
    <div class="bg-white p-6 rounded-lg shadow-md">
      <form action="/efile" method="POST" enctype="multipart/form-data">
        <div class="mb-4">
          <label for="caseId" class="block text-gray-700">Case</label>
          <select name="caseId" id="caseId" class="w-full border rounded p-2" required>
            <% cases.forEach(caseRecord => { %>
              <option value="<%= caseRecord.id %>"><%= caseRecord.caseNumber %></option>
            <% }) %>
          </select>
        </div>
        <div class="mb-4">
          <label for="document" class="block text-gray-700">Document</label>
          <input type="file" name="document" id="document" class="w-full border rounded p-2" required>
        </div>
        <button type="submit" class="bg-blue-500 text-white p-2 rounded">E-file</button>
      </form>
    </div>
  </div>
</body>
</html>
