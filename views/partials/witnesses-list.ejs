<div class="flex justify-between items-center mb-4">
    <h2 class="text-xl font-bold">Witnesses</h2>
</div>
<% if (caseData.witnesses && caseData.witnesses.length > 0) { %>
  <ul class="divide-y divide-gray-200">
    <% caseData.witnesses.forEach(w => { %>
      <li class="py-2"><%= w.name %> — <%= w.statement || 'No statement' %></li>
    <% }) %>
  </ul>
<% } else { %>
  <div class="text-center py-8 bg-gray-50 rounded-lg">
    <p class="text-gray-500">No witnesses have been added yet.</p>
  </div>
<% } %>
