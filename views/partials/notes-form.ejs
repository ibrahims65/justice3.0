<form action="/bookings/<%= booking.id %>/notes" method="POST">
  <textarea name="officerNotes" placeholder="Officer Notes"><%= booking.officerNotes %></textarea>
  <button type="submit">Save Notes</button>
</form>
