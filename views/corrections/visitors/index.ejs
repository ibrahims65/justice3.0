<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Visitor Management</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <%- include('../../partials/navbar', { user: user, page: '/corrections/visitors' }) %>
  <div class="container mx-auto p-8">
    <div class="flex justify-between items-center mb-4">
      <h1 class="text-2xl font-bold">Visitor Management</h1>
      <a href="/corrections/visitors/new" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">New Visitor</a>
    </div>
    <div class="bg-white p-6 rounded-lg shadow-md">
      <table class="w-full">
        <thead>
          <tr>
            <th class="text-left">Name</th>
            <th class="text-left">Email</th>
            <th class="text-left">Phone</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% visitors.forEach(visitor => { %>
            <tr>
              <td><%= visitor.name %></td>
              <td><%= visitor.email %></td>
              <td><%= visitor.phone %></td>
              <td>
                <a href="/corrections/visitors/<%= visitor.id %>/edit" class="text-blue-500 hover:underline">Edit</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
