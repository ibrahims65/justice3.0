<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Corrections Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <%- include('../partials/navbar', { user: user, page: page }) %>

  <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
    <%- include('../partials/breadcrumbs', { breadcrumbs: breadcrumbs }) %>
    <div class="px-4 py-6 sm:px-0">
      <h2 class="text-2xl font-bold text-gray-800 mb-4">Welcome, <%= user.username %>!</h2>
      <p class="text-gray-600 mb-8">Your role is: <%= user.role.name %></p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-white rounded-lg shadow-md p-6">
          <h3 class="text-lg font-medium text-gray-800">Inmate Population</h3>
          <p class="text-3xl font-bold text-blue-500"><%= inmates.length %></p>
        </div>
        <div class="bg-white rounded-lg shadow-md p-6">
          <h3 class="text-lg font-medium text-gray-800">Transfer Requests</h3>
          <p class="text-3xl font-bold text-blue-500"><%= transferRequests.length %></p>
        </div>
        <div class="bg-white rounded-lg shadow-md p-6">
          <h3 class="text-lg font-medium text-gray-800">Remand Outcomes</h3>
          <p class="text-3xl font-bold text-blue-500"><%= remandOutcomes.length %></p>
        </div>
      </div>

      <div class="mt-8 bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-medium text-gray-800 mb-4">Transfer Requests</h3>
        <p>Not yet implemented.</p>
      </div>

      <div class="mt-8 bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-medium text-gray-800 mb-4">Remand Outcomes</h3>
        <p>Not yet implemented.</p>
      </div>

      <div class="mt-8 bg-white rounded-lg shadow-md p-6">
        <h3 class="text-lg font-medium text-gray-800 mb-4">Inmate List</h3>
        <% if (inmates.length > 0) { %>
          <table class="w-full text-left">
            <thead>
              <tr>
                <th class="py-2">Name</th>
                <th class="py-2">Booking ID</th>
                <th class="py-2">Status</th>
              </tr>
            </thead>
            <tbody>
              <% inmates.forEach(inmate => { %>
                <tr class="border-b border-gray-200">
                  <td class="py-2"><a href="/people/<%= inmate.id %>" class="text-blue-500 hover:underline"><%= inmate.name %></a></td>
                  <td class="py-2"><%= inmate.bookings[0].id %></td>
                  <td class="py-2"><%= inmate.bookings[0].status %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <p>No inmates found.</p>
        <% } %>
      </div>

    </div>
  </div>
</body>
</html>
